<!--pages/home/home.wxml-->
<text>wxs语法</text>
<!-- WXS 的作用 在wxml中不能直接使用js的函数 利用wxs就可以调用 -->

<!-- 显示两位小数点 -->
<!-- <view>{{25.666666666.toFixed()}}---错误的写法</view> -->

<!-- wxs的写法 -->
<!-- 1.在wxs标签中写 不过wxs 标签中必须有 module 属性, 要想wxs模块外使用 wxs 里的函数 必须要导出函数 -->
<!-- <wxs module="info">
  var fixed = function(num) {
    return num.toFixed(2)
  }
  module.exports = {
  fixed: fixed
  }
</wxs> -->

<!-- 2. 单独建一个wxs文件 -->
<!-- 导入单独的wxs文件 src属性只支持相对路径-->
<wxs src="../../wxs/info.wxs" module="info"/>
<view>{{info.fixed(25.666666)}}</view>


<!-- wxs的应用 -->
<wxs src="../../wxs/format.wxs" module="format"/>
<view>{{format.priceFormat(price,3)}}</view>